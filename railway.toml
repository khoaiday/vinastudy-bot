[build]
builder = "NIXPACKS"

[deploy]
# startCommand được set riêng trong từng Railway service:
#   vinastudy-bot → python bot.py
#   worker (admin) → python bot_admin.py
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
